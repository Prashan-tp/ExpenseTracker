--Take a pull of the code 
--open the code in editor
--Also open a terminal

--Go to backend folder from terminal
         cd expense_tracker_backend
         
--Go to frontend folder from terminal
         cd expense_tracker_frontend

--Install ndoe_module package by using (in both the folder)
       npm install 

-- to start backend server 
   nodemon index.js

-- to start frontend server
      npm start
      

